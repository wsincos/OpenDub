# Authorized Demo

This example is generated at build time from FFmpeg test-pattern video and a synthetic sine-wave
audio reference. It is intentionally not a demonstration of a real voice model. Its purpose is
to verify the current alpha path: local video, an explicit self-recorded declaration, an audio
reference, and a ready timeline segment.

