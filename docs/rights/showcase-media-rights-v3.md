# Case 03 Showcase Media Authorization Record

**Record date:** 2026-07-27
**Issuing role:** OpenDub project owner / repository maintainer
**Applies to:** `content/showcases/v3/case-03.json`

## Authorized use

The project owner authorizes the four exact Case 03 media artifacts identified by the case manifest SHA-256 values for the following limited uses:

1. public display in the `wsincos/OpenDub` repository and OpenDub project demonstration pages;
2. inclusion in the OpenDub youth open-source seed-plan application video and supporting application material.

The authorization follows the project-owner provision of the Case 03 artifacts for this implementation request. It covers only the listed media files and their generated poster, waveform, mel, F0, energy, and provenance derivatives. It does not grant a general right to distribute source footage, identities, voices, datasets, checkpoints, or unrelated downstream outputs.

## Required public boundary

- Case 03 must remain labelled `Archived same-scene listening` and `Historical team-provided result. Not a fresh OpenDub run.`
- The bundle must not be described as a verified common-input benchmark, `Replay`, `Live`, a public checkpoint, or a newly generated grant result.
- The canonical transcript, reference-speech contract, precise historical input fingerprint, method revision, and evaluation protocol are not published with this bundle. The UI must state these absences rather than infer them.
- The repository must retain the case manifest, this record, source and public hashes, generated provenance, and the project contact route in repository governance files.
- If the project owner withdraws authorization or identifies a conflicting upstream right, maintainers must remove the Case 03 media, derivatives, and application-video segment, then change the case state to `blocked`.

## Verification note

This is a project-owner authorization record for a bounded historical display. It is not an independent copyright-chain audit and does not establish cross-method fairness. The separate manifest and deterministic verification process exist so viewers can distinguish an authorized archive from a reproducible benchmark or a live inference claim.
